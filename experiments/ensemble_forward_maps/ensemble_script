#!/bin/bash

#Submit this script with: sbatch ensemble_script

#SBATCH --time=24:00:00   # walltime
#SBATCH --mem-per-cpu=30G   # memory per CPU core
#SBATCH --nodes=1         # number of nodes
#SBATCH --ntasks=41  # number of processor cores (i.e. tasks)
#SBATCH -J "scm_ens"   # job name

julia --project -e 'using Pkg; Pkg.instantiate(); Pkg.API.precompile()'

julia --project -p 41 parallel_ensemble_runs.jl